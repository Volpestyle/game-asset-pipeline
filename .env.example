# ==== API / Web ====
API_PORT=4000
WEB_PORT=3000
API_BASE_URL=http://localhost:4000

# ==== Storage ====
DATA_DIR=./data

# ==== Queue / Redis (BullMQ) ====
REDIS_URL=redis://localhost:6379

# ==== Database (Prisma / SQLite) ====
DATABASE_URL="file:./dev.sqlite"

# ==== Model Providers ====
# FAL
FAL_KEY=""

# Replicate
REPLICATE_API_TOKEN=""

# Use mock provider to run end-to-end without keys
PIPELINE_PROVIDER=mock
# (or "fal" or "replicate")

# Model manifest path (capability -> provider endpoint or model)
MODEL_MANIFEST_PATH=./config/model-manifest.json

# Next.js (client-side exposed)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
